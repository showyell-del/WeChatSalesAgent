---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Phase0App/Info.plist
  - app/Phase0App/main.m
  - scripts/build_phase0_app.sh
autonomous: true
requirements:
  - Platform boundary
  - Exact WeChat 4.1.11.55 build 269111 profile
  - Signed/notarized commercial feasibility gate
user_setup: []
must_haves:
  truths:
    - "scripts/build_phase0_app.sh creates an ad-hoc signed AppKit .app bundle."
    - "The app skeleton is native AppKit and contains no WebView, HTTP server, Electron, Tauri, or browser UI entrypoint."
    - "The app smoke command emits PHASE0_APP_LAUNCHED as JSON."
  artifacts:
    - path: "app/Phase0App/main.m"
      provides: "Native AppKit entry point"
      contains: "PHASE0_APP_LAUNCHED"
    - path: "app/Phase0App/Info.plist"
      provides: "macOS app bundle metadata"
      contains: "LSMinimumSystemVersion"
    - path: "scripts/build_phase0_app.sh"
      provides: "clang build and ad-hoc signing"
      contains: "/usr/bin/clang"
  key_links:
    - from: "scripts/build_phase0_app.sh"
      to: "app/Phase0App/main.m"
      via: "clang build command"
      pattern: "app/Phase0App/main.m"
---

<objective>
Create the minimal native AppKit `.app` skeleton and launch diagnostic that all Phase 0 gates will use.

Purpose: Establish the commercial desktop foundation using verified local tools, without Web UI or unavailable Go build steps.
Output: A buildable native macOS app bundle with an AppKit entrypoint, Info.plist, ad-hoc signing path, and explicit launch diagnostic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-VALIDATION.md
@Agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppKit app bundle source</name>
  <files>app/Phase0App/Info.plist, app/Phase0App/main.m</files>
  <read_first>.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md, Agent.md</read_first>
  <action>Create `app/Phase0App/Info.plist` with `CFBundleExecutable` `WeChatSalesAgent`, bundle id `com.local.wechat-sales-agent.phase0`, and `LSMinimumSystemVersion` `15.0`. Create `app/Phase0App/main.m` using AppKit/Cocoa, printing JSON code `PHASE0_APP_LAUNCHED`, opening a minimal native window, and exiting immediately when invoked with `--smoke`. Do not add WebKit, Vapor, Hummingbird, Electron, Tauri, Node, Go, Swift, or local HTTP server dependencies.</action>
  <verify>scripts/build_phase0_app.sh && dist/phase0/WeChatSalesAgent.app/Contents/MacOS/WeChatSalesAgent --smoke</verify>
  <acceptance_criteria>
    - `app/Phase0App/Info.plist` contains `LSMinimumSystemVersion`.
    - `app/Phase0App/main.m` contains `PHASE0_APP_LAUNCHED`.
    - `scripts/build_phase0_app.sh` contains `/usr/bin/clang`.
    - `dist/phase0/WeChatSalesAgent.app/Contents/MacOS/WeChatSalesAgent --smoke` output contains `PHASE0_APP_LAUNCHED`.
    - `rg -n "WebView|WKWebView|Vapor|Hummingbird|Electron|Tauri|localhost|HTTPServer" app native-worker scripts -g '!scripts/phase0_validate.sh'` returns no matches.
  </acceptance_criteria>
  <done>AppKit `.app` source exists, native executable builds, and no Web UI/server dependency is introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/build_phase0_app.sh` exits 0.
- [ ] `dist/phase0/WeChatSalesAgent.app/Contents/MacOS/WeChatSalesAgent --smoke` outputs `PHASE0_APP_LAUNCHED`.
- [ ] `rg -n "WebView|WKWebView|Vapor|Hummingbird|Electron|Tauri|localhost|HTTPServer" app native-worker scripts -g '!scripts/phase0_validate.sh'` returns no matches.
</verification>

<success_criteria>
- Native AppKit `.app` skeleton exists.
- App launch emits explicit pass diagnostic JSON.
- Build and signing use `/usr/bin/clang` and `/usr/bin/codesign`.
- No Web UI/server or Go build dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-01-SUMMARY.md`.
</output>
