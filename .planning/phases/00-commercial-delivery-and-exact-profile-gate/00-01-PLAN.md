---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Sources/AgentCore/Diagnostics.swift
  - Sources/AgentCore/ProcessRunner.swift
  - Sources/WeChatSalesAgentApp/main.swift
  - Tests/AgentCoreTests/DiagnosticsTests.swift
autonomous: true
requirements:
  - Platform boundary
  - Exact WeChat 4.1.11.55 build 269111 profile
  - Signed/notarized commercial feasibility gate
user_setup: []
must_haves:
  truths:
    - "swift test exits 0 on the Phase 0 skeleton."
    - "The app skeleton is native Swift and contains no WebView, HTTP server, Electron, Tauri, or browser UI entrypoint."
    - "Diagnostic states use explicit terminal results: passed, failed, blocked."
  artifacts:
    - path: "Package.swift"
      provides: "Swift package with app, core library, and tests"
      contains: "WeChatSalesAgentApp"
    - path: "Sources/AgentCore/Diagnostics.swift"
      provides: "Typed diagnostic result model"
      contains: "enum DiagnosticStatus"
    - path: "Sources/WeChatSalesAgentApp/main.swift"
      provides: "Native Swift entry point"
      contains: "@main"
  key_links:
    - from: "Sources/WeChatSalesAgentApp/main.swift"
      to: "Sources/AgentCore/Diagnostics.swift"
      via: "imports AgentCore"
      pattern: "import AgentCore"
---

<objective>
Create the minimal native Swift package skeleton and typed diagnostic model that all Phase 0 gates will use.

Purpose: Establish the commercial desktop foundation using verified local tools, without Web UI or unavailable Go build steps.
Output: A buildable Swift package with a native app entrypoint, reusable AgentCore library, command runner abstraction, and tests for explicit diagnostic states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-VALIDATION.md
@Agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Swift package structure</name>
  <files>Package.swift, Sources/AgentCore/Diagnostics.swift, Sources/AgentCore/ProcessRunner.swift, Sources/WeChatSalesAgentApp/main.swift, Tests/AgentCoreTests/DiagnosticsTests.swift</files>
  <read_first>.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md, Agent.md</read_first>
  <action>Create `Package.swift` with package name `WeChatSalesAgent`, platforms `.macOS(.v15)`, a library target `AgentCore`, an executable target `WeChatSalesAgentApp` depending on `AgentCore`, and a test target `AgentCoreTests`. Create `Sources/WeChatSalesAgentApp/main.swift` with a native Swift `@main` entrypoint that prints one diagnostic banner line `WeChat Sales Agent Phase 0` and imports `AgentCore`. Do not add WebKit, Vapor, Hummingbird, Electron, Tauri, Node, Go, or local HTTP server dependencies.</action>
  <verify>swift test</verify>
  <acceptance_criteria>
    - `Package.swift` contains `name: "WeChatSalesAgent"`.
    - `Package.swift` contains `.macOS(.v15)`.
    - `Package.swift` contains `.executableTarget(`.
    - `Sources/WeChatSalesAgentApp/main.swift` contains `@main`.
    - `Sources/WeChatSalesAgentApp/main.swift` contains `import AgentCore`.
    - `rg -n "WebView|WKWebView|Vapor|Hummingbird|Electron|Tauri|localhost|HTTPServer" Package.swift Sources Tests` returns no matches.
  </acceptance_criteria>
  <done>Swift package exists, native executable target builds, and no Web UI/server dependency is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed diagnostic results</name>
  <files>Sources/AgentCore/Diagnostics.swift, Tests/AgentCoreTests/DiagnosticsTests.swift</files>
  <read_first>Sources/AgentCore/Diagnostics.swift, Tests/AgentCoreTests/DiagnosticsTests.swift, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md</read_first>
  <action>Implement `public enum DiagnosticStatus: String, Codable, Equatable` with exact cases `passed`, `failed`, and `blocked`. Implement `public struct DiagnosticEvent: Codable, Equatable` with properties `step: String`, `status: DiagnosticStatus`, `code: String`, `message: String`, and `evidence: [String: String]`. Add static constructors `passed(step:code:message:evidence:)`, `failed(step:code:message:evidence:)`, and `blocked(step:code:message:evidence:)`. Tests must encode a blocked event and assert the JSON contains `"status":"blocked"` and `"code":"DEVELOPER_ID_IDENTITY_MISSING"`.</action>
  <verify>swift test --filter DiagnosticsTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/Diagnostics.swift` contains `enum DiagnosticStatus`.
    - `Sources/AgentCore/Diagnostics.swift` contains `case blocked`.
    - `Sources/AgentCore/Diagnostics.swift` contains `struct DiagnosticEvent`.
    - `Tests/AgentCoreTests/DiagnosticsTests.swift` contains `DEVELOPER_ID_IDENTITY_MISSING`.
    - `swift test --filter DiagnosticsTests` exits 0.
  </acceptance_criteria>
  <done>Diagnostic events can represent pass, fail, and blocked gates without fallback ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Add process runner abstraction</name>
  <files>Sources/AgentCore/ProcessRunner.swift, Tests/AgentCoreTests/DiagnosticsTests.swift</files>
  <read_first>Sources/AgentCore/ProcessRunner.swift, Sources/AgentCore/Diagnostics.swift</read_first>
  <action>Implement `public struct ProcessResult: Equatable` with `exitCode: Int32`, `stdout: String`, and `stderr: String`. Implement `public protocol ProcessRunning` with `func run(_ executable: String, _ arguments: [String]) throws -> ProcessResult`. Implement `public struct LocalProcessRunner: ProcessRunning` using `Process` and `Pipe` without shell interpolation. Add a test-only fake runner in `DiagnosticsTests.swift` that returns a configured `ProcessResult` and proves callers can classify command output without invoking shell strings.</action>
  <verify>swift test --filter DiagnosticsTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/ProcessRunner.swift` contains `protocol ProcessRunning`.
    - `Sources/AgentCore/ProcessRunner.swift` contains `LocalProcessRunner`.
    - `Sources/AgentCore/ProcessRunner.swift` contains `Process()`.
    - `Sources/AgentCore/ProcessRunner.swift` does not contain `/bin/sh`.
    - `swift test --filter DiagnosticsTests` exits 0.
  </acceptance_criteria>
  <done>Later gates can run local tools safely without shell-string fallback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test` exits 0.
- [ ] `rg -n "WebView|WKWebView|Vapor|Hummingbird|Electron|Tauri|localhost|HTTPServer" Package.swift Sources Tests` returns no matches.
- [ ] `rg -n "go build|/usr/local/go|/opt/homebrew/bin/go" Package.swift Sources Tests` returns no matches.
</verification>

<success_criteria>
- Native Swift package skeleton exists.
- Typed diagnostic results encode explicit pass/fail/blocked terminal states.
- Process execution is argument-array based and does not use shell interpolation.
- No Web UI/server or Go build dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-01-SUMMARY.md`.
</output>
