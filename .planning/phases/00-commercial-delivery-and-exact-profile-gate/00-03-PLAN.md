---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 03
type: execute
wave: 2
depends_on: ["00-01", "00-02"]
files_modified:
  - native-worker/phase0_worker.py
  - native-worker/probes/attach_smoke.js
  - native-worker/probes/text_send_smoke.js
  - scripts/phase0_worker.sh
  - scripts/phase0_chatlog_smoke.sh
autonomous: true
requirements:
  - Platform boundary
  - Exact WeChat 4.1.11.55 build 269111 profile
user_setup:
  - "Live attach/key/read/text-send smoke requires the user to have WeChat 4.1.11.55 logged in with an acceptable local security posture."
must_haves:
  truths:
    - "Worker version command prints JSON with frida_version 16.7.19."
    - "Worker reports WECHAT_NOT_RUNNING distinctly when no WeChat process is available."
    - "Chatlog smoke script classifies missing binary, missing service, and callable help commands explicitly."
    - "Text-send smoke is opt-in and targets filehelper only."
  artifacts:
    - path: "native-worker/phase0_worker.py"
      provides: "Python worker command entrypoint"
      contains: "frida_version"
    - path: "scripts/phase0_worker.sh"
      provides: "Worker wrapper script"
      contains: "phase0_worker.py"
    - path: "scripts/phase0_chatlog_smoke.sh"
      provides: "Chatlog key/read smoke wrapper"
      contains: "chatlog-darwin-arm64"
  key_links:
    - from: "scripts/phase0_worker.sh"
      to: "native-worker/phase0_worker.py"
      via: "executes python worker"
      pattern: "phase0_worker.py"
---

<objective>
Create the minimal local worker and Chatlog smoke wrappers needed to prove Phase 0 lifecycle boundaries.

Purpose: Establish a narrow, testable worker command contract before any product feature depends on native operations.
Output: Python worker command entrypoint, attach/text smoke probe assets, and shell wrappers that emit explicit JSON/JSONL terminal states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@Agent.md
@/tmp/chatlog-alpha-spike.WWc9fK/repo/internal/wechat/send/assets/native_send_once.py
@/tmp/chatlog-alpha-spike.WWc9fK/repo/internal/wechat/send/assets/native/single_send_agent.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement worker command entrypoint</name>
  <files>native-worker/phase0_worker.py, scripts/phase0_worker.sh</files>
  <read_first>.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md, Agent.md</read_first>
  <action>Create `native-worker/phase0_worker.py` with argparse subcommands `version`, `probe-process`, `attach-smoke`, `cleanup-smoke`, and `text-send-smoke-manifest`. Every subcommand must print one JSON object per line with keys `step`, `status`, `code`, `message`, and `evidence`. The `version` command must import `frida` and emit evidence key `frida_version`. If import fails, emit `status = "failed"` and `code = "FRIDA_IMPORT_FAILED"`. Create `scripts/phase0_worker.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and invocation of `python3 native-worker/phase0_worker.py "$@"`.</action>
  <verify>python3 native-worker/phase0_worker.py version && scripts/phase0_worker.sh version</verify>
  <acceptance_criteria>
    - `native-worker/phase0_worker.py` contains `FRIDA_IMPORT_FAILED`.
    - `native-worker/phase0_worker.py` contains `frida_version`.
    - `native-worker/phase0_worker.py` contains `text-send-smoke-manifest`.
    - `scripts/phase0_worker.sh` contains `set -euo pipefail`.
    - `python3 native-worker/phase0_worker.py version` output contains `"frida_version": "16.7.19"`.
  </acceptance_criteria>
  <done>Worker can report its local Frida capability in machine-readable form.</done>
</task>

<task type="auto">
  <name>Task 2: Add attach lifecycle smoke probe</name>
  <files>native-worker/phase0_worker.py, native-worker/probes/attach_smoke.js</files>
  <read_first>native-worker/phase0_worker.py, .planning/spikes/001-native-file-send/profile_probe.py, Agent.md</read_first>
  <action>Create `native-worker/probes/attach_smoke.js` with a single exported RPC method `ping` returning `{ ok: true, probe: "attach_smoke" }`. Implement `attach-smoke` in `phase0_worker.py` to locate the WeChat process by exact executable name/path, return blocked `WECHAT_NOT_RUNNING` if absent, attach with Frida only when present, load `attach_smoke.js`, call `ping`, unload, detach, and emit `ATTACH_SMOKE_PASSED` or a precise failure code. Do not kill WeChat. Do not retry through another target.</action>
  <verify>python3 native-worker/phase0_worker.py attach-smoke || true</verify>
  <acceptance_criteria>
    - `native-worker/probes/attach_smoke.js` contains `attach_smoke`.
    - `native-worker/phase0_worker.py` contains `WECHAT_NOT_RUNNING`.
    - `native-worker/phase0_worker.py` contains `ATTACH_SMOKE_PASSED`.
    - `native-worker/phase0_worker.py` does not contain `Hermes`.
    - `python3 native-worker/phase0_worker.py attach-smoke` exits 0 when it reports `WECHAT_NOT_RUNNING` as a blocked terminal state.
  </acceptance_criteria>
  <done>Attach lifecycle command is exact, non-destructive, and can block cleanly when WeChat is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Add Chatlog key/read smoke wrapper</name>
  <files>scripts/phase0_chatlog_smoke.sh</files>
  <read_first>chatlog_2f54920_darwin_arm64/README.md, Agent.md, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md</read_first>
  <action>Create `scripts/phase0_chatlog_smoke.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must define `CHATLOG_BIN="./chatlog_2f54920_darwin_arm64/chatlog-darwin-arm64"`, support `--help-smoke` and `--http-list-smoke`, and print JSONL events with exact codes `CHATLOG_BINARY_MISSING`, `CHATLOG_HELP_CALLABLE`, `CHATLOG_HTTP_LIST_CALLABLE`, and `CHATLOG_SERVICE_UNAVAILABLE`. `--help-smoke` must call `"$CHATLOG_BIN" key --help` and `"$CHATLOG_BIN" http --help`. `--http-list-smoke` must call `"$CHATLOG_BIN" http list` and classify non-zero output as `CHATLOG_SERVICE_UNAVAILABLE` rather than claiming read success.</action>
  <verify>scripts/phase0_chatlog_smoke.sh --help-smoke</verify>
  <acceptance_criteria>
    - `scripts/phase0_chatlog_smoke.sh` contains `CHATLOG_BIN="./chatlog_2f54920_darwin_arm64/chatlog-darwin-arm64"`.
    - `scripts/phase0_chatlog_smoke.sh` contains `CHATLOG_HELP_CALLABLE`.
    - `scripts/phase0_chatlog_smoke.sh` contains `CHATLOG_SERVICE_UNAVAILABLE`.
    - `scripts/phase0_chatlog_smoke.sh --help-smoke` output contains `CHATLOG_HELP_CALLABLE`.
  </acceptance_criteria>
  <done>Chatlog smoke wrapper proves command availability and classifies service absence explicitly.</done>
</task>

<task type="auto">
  <name>Task 4: Add text-send smoke manifest only</name>
  <files>native-worker/phase0_worker.py, native-worker/probes/text_send_smoke.js</files>
  <read_first>/tmp/chatlog-alpha-spike.WWc9fK/repo/internal/wechat/send/assets/native_send_once.py, /tmp/chatlog-alpha-spike.WWc9fK/repo/internal/wechat/send/assets/native/single_send_agent.js, Agent.md</read_first>
  <action>Create `native-worker/probes/text_send_smoke.js` as a placeholder probe file containing `const PHASE0_TEXT_SEND_TARGET = "filehelper";` and comments naming the upstream references. Implement `text-send-smoke-manifest` in `phase0_worker.py` to emit blocked JSON code `TEXT_SEND_SMOKE_MANUAL_REQUIRED` with evidence keys `target = "filehelper"` and `requires = "exact_profile_logged_in_wechat_visible_ack_cleanup"`. Do not implement customer sending, image, video, or file send here. Do not include `uploadappattach` or `sendappmsg` in this Phase 0 production worker path.</action>
  <verify>python3 native-worker/phase0_worker.py text-send-smoke-manifest && rg -n "uploadappattach|sendappmsg|Hermes|clipboard|share sheet" native-worker scripts || true</verify>
  <acceptance_criteria>
    - `native-worker/probes/text_send_smoke.js` contains `PHASE0_TEXT_SEND_TARGET = "filehelper"`.
    - `native-worker/phase0_worker.py` contains `TEXT_SEND_SMOKE_MANUAL_REQUIRED`.
    - `python3 native-worker/phase0_worker.py text-send-smoke-manifest` output contains `"target": "filehelper"`.
    - `rg -n "uploadappattach|sendappmsg|Hermes|clipboard|share sheet" native-worker scripts` returns no matches.
  </acceptance_criteria>
  <done>Text-send smoke is represented as an explicit opt-in manual gate and does not expose unsupported attachment paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 native-worker/phase0_worker.py version` outputs `"frida_version": "16.7.19"`.
- [ ] `python3 native-worker/phase0_worker.py attach-smoke` exits 0 with either `ATTACH_SMOKE_PASSED` or blocked `WECHAT_NOT_RUNNING`.
- [ ] `scripts/phase0_chatlog_smoke.sh --help-smoke` outputs `CHATLOG_HELP_CALLABLE`.
- [ ] `rg -n "uploadappattach|sendappmsg|Hermes|clipboard|share sheet" native-worker scripts` returns no matches.
</verification>

<success_criteria>
- Worker command contract exists and is machine-readable.
- Attach lifecycle can be tested without fallback and without killing WeChat.
- Chatlog key/read smoke wrapper classifies missing preconditions.
- Text send smoke remains filehelper-only and manual until live verified.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-03-SUMMARY.md`.
</output>
