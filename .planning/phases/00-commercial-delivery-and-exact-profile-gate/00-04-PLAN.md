---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 04
type: execute
wave: 3
depends_on: ["00-01", "00-02", "00-03"]
files_modified:
  - Sources/AgentCore/PackageGate.swift
  - Tests/AgentCoreTests/PackageGateTests.swift
  - scripts/build_phase0_app.sh
  - scripts/phase0_validate.sh
  - Agent.md
autonomous: true
requirements:
  - Platform boundary
  - Signed/notarized commercial feasibility gate
  - Exact WeChat 4.1.11.55 build 269111 profile
user_setup:
  - "Developer ID notarization remains blocked until a valid Developer ID Application identity and notary credentials are installed."
  - "Clean-Mac install verification requires a separate clean Apple Silicon Mac."
must_haves:
  truths:
    - "Package gate reports DEVELOPER_ID_IDENTITY_MISSING when no valid signing identity exists."
    - "Ad-hoc signing is labeled local-only and never commercial-ready."
    - "Full validation script produces JSONL with package, profile, worker, chatlog, send-smoke-manifest, and no-fallback scan events."
    - "Agent.md records newly verified Phase 0 commands and blocked commercial prerequisites."
  artifacts:
    - path: "Sources/AgentCore/PackageGate.swift"
      provides: "Package/signing gate classification"
      contains: "DEVELOPER_ID_IDENTITY_MISSING"
    - path: "scripts/phase0_validate.sh"
      provides: "Unified Phase 0 validator"
      contains: "--no-fallback-scan"
    - path: "scripts/build_phase0_app.sh"
      provides: "Local Phase 0 build/sign command"
      contains: "codesign"
  key_links:
    - from: "scripts/phase0_validate.sh"
      to: "scripts/phase0_worker.sh"
      via: "worker validation command"
      pattern: "phase0_worker.sh"
    - from: "scripts/phase0_validate.sh"
      to: "scripts/phase0_chatlog_smoke.sh"
      via: "chatlog validation command"
      pattern: "phase0_chatlog_smoke.sh"
---

<objective>
Implement Phase 0 packaging diagnostics, unified validation, and Agent.md evidence update.

Purpose: Make commercial readiness honest: local build and ad-hoc validation can pass now, while Developer ID notarization and clean-Mac install are explicit blocked/manual gates until credentials and target hardware exist.
Output: Package gate model/tests, build script, validation script, and updated project notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-VALIDATION.md
@Agent.md
@Sources/AgentCore/Diagnostics.swift
@native-worker/phase0_worker.py
@scripts/phase0_chatlog_smoke.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement package gate classification</name>
  <files>Sources/AgentCore/PackageGate.swift, Tests/AgentCoreTests/PackageGateTests.swift</files>
  <read_first>Sources/AgentCore/Diagnostics.swift, Sources/AgentCore/ProcessRunner.swift, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md</read_first>
  <action>Create `public struct PackageGate` with initializer `init(runner: ProcessRunning)` and method `public func developerIDIdentityStatus() -> DiagnosticEvent`. It must run `/usr/bin/security` with arguments `["find-identity", "-v", "-p", "codesigning"]` through `ProcessRunning`. If stdout contains `0 valid identities found`, return blocked event `code = "DEVELOPER_ID_IDENTITY_MISSING"` and message `Developer ID Application identity is required for notarization`. If stdout contains `Developer ID Application`, return passed `code = "DEVELOPER_ID_IDENTITY_AVAILABLE"`. Add tests with fake runner output for both cases.</action>
  <verify>swift test --filter PackageGateTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/PackageGate.swift` contains `DEVELOPER_ID_IDENTITY_MISSING`.
    - `Sources/AgentCore/PackageGate.swift` contains `Developer ID Application`.
    - `Tests/AgentCoreTests/PackageGateTests.swift` contains `testMissingDeveloperIDBlocksNotarization`.
    - `swift test --filter PackageGateTests` exits 0.
  </acceptance_criteria>
  <done>Commercial signing prerequisite is an explicit blocked diagnostic when credentials are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add local build and ad-hoc sign script</name>
  <files>scripts/build_phase0_app.sh</files>
  <read_first>Package.swift, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md</read_first>
  <action>Create `scripts/build_phase0_app.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must run `swift build -c release`, create `dist/phase0/`, copy `.build/release/WeChatSalesAgentApp` to `dist/phase0/WeChatSalesAgentApp`, run `/usr/bin/codesign -s - --force dist/phase0/WeChatSalesAgentApp`, and print JSONL event `AD_HOC_SIGNED_LOCAL_ONLY`. It must check Developer ID identity with `/usr/bin/security find-identity -v -p codesigning` and print `DEVELOPER_ID_IDENTITY_MISSING` when absent. Do not call `notarytool submit` unless identity is present and explicit credentials env vars `PHASE0_NOTARY_PROFILE` or `APPLE_ID`/`APPLE_TEAM_ID` are present.</action>
  <verify>scripts/build_phase0_app.sh</verify>
  <acceptance_criteria>
    - `scripts/build_phase0_app.sh` contains `swift build -c release`.
    - `scripts/build_phase0_app.sh` contains `/usr/bin/codesign -s - --force`.
    - `scripts/build_phase0_app.sh` contains `AD_HOC_SIGNED_LOCAL_ONLY`.
    - `scripts/build_phase0_app.sh` contains `DEVELOPER_ID_IDENTITY_MISSING`.
    - `scripts/build_phase0_app.sh` does not contain unconditional `notarytool submit`.
  </acceptance_criteria>
  <done>Local build produces an ad-hoc signed artifact and labels it honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement unified Phase 0 validator</name>
  <files>scripts/phase0_validate.sh</files>
  <read_first>scripts/phase0_worker.sh, scripts/phase0_chatlog_smoke.sh, scripts/build_phase0_app.sh, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-VALIDATION.md</read_first>
  <action>Create `scripts/phase0_validate.sh` with subcommands `--quick`, `--full`, `--package`, `--worker`, `--chatlog`, `--send-smoke-manifest`, and `--no-fallback-scan`. `--quick` must run `swift test` and `--no-fallback-scan`. `--full` must run `swift test`, `--package`, `--worker`, `--chatlog`, `--send-smoke-manifest`, and `--no-fallback-scan`. `--no-fallback-scan` must scan only production paths `Package.swift Sources Tests native-worker scripts` and fail if it finds `Hermes`, `clipboard`, `key simulation`, `share sheet`, `uploadappattach`, or `sendappmsg`. The script must print JSONL event codes for every subcommand and exit non-zero only on real failed checks, not on explicit blocked manual gates.</action>
  <verify>scripts/phase0_validate.sh --quick && scripts/phase0_validate.sh --full</verify>
  <acceptance_criteria>
    - `scripts/phase0_validate.sh` contains `--no-fallback-scan`.
    - `scripts/phase0_validate.sh` contains `Hermes`.
    - `scripts/phase0_validate.sh` contains `uploadappattach`.
    - `scripts/phase0_validate.sh` contains `phase0_worker.sh`.
    - `scripts/phase0_validate.sh` contains `phase0_chatlog_smoke.sh`.
    - `scripts/phase0_validate.sh --quick` exits 0.
  </acceptance_criteria>
  <done>One command validates build, gates, worker, Chatlog smoke, send smoke manifest, and forbidden production paths.</done>
</task>

<task type="auto">
  <name>Task 4: Update Agent.md with verified Phase 0 commands</name>
  <files>Agent.md</files>
  <read_first>Agent.md, scripts/phase0_validate.sh, scripts/build_phase0_app.sh</read_first>
  <action>After the Phase 0 validation commands pass or return explicit blocked states, append a section `## Phase 0 verified commands` to `Agent.md`. Include exact commands: `swift test`, `scripts/phase0_validate.sh --quick`, `scripts/phase0_validate.sh --full`, `scripts/build_phase0_app.sh`, `python3 native-worker/phase0_worker.py version`, and `scripts/phase0_chatlog_smoke.sh --help-smoke`. Record that Developer ID notarization remains blocked while `security find-identity -v -p codesigning` reports `0 valid identities found`.</action>
  <verify>rg -n "Phase 0 verified commands|scripts/phase0_validate.sh --full|DEVELOPER_ID_IDENTITY_MISSING" Agent.md</verify>
  <acceptance_criteria>
    - `Agent.md` contains `## Phase 0 verified commands`.
    - `Agent.md` contains `scripts/phase0_validate.sh --full`.
    - `Agent.md` contains `scripts/build_phase0_app.sh`.
    - `Agent.md` contains `DEVELOPER_ID_IDENTITY_MISSING`.
  </acceptance_criteria>
  <done>Project notes capture newly verified Phase 0 paths and remaining commercial credential gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test` exits 0.
- [ ] `scripts/phase0_validate.sh --quick` exits 0.
- [ ] `scripts/phase0_validate.sh --full` exits 0 or reports only documented blocked manual/commercial gates.
- [ ] `scripts/build_phase0_app.sh` creates `dist/phase0/WeChatSalesAgentApp`.
- [ ] `rg -n "Phase 0 verified commands" Agent.md` finds the evidence section.
</verification>

<success_criteria>
- Developer ID absence is detected and not mislabeled as commercial-ready.
- Ad-hoc local artifact can be built and signed for local verification.
- Unified validation covers every Phase 0 gate.
- Agent.md records verified commands and blocked prerequisites.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-04-SUMMARY.md`.
</output>
