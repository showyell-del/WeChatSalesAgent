---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 02
type: execute
wave: 1
depends_on: ["00-01"]
files_modified:
  - native-worker/phase0_worker.py
  - scripts/phase0_validate.sh
autonomous: true
requirements:
  - Exact WeChat 4.1.11.55 build 269111 profile
user_setup: []
must_haves:
  truths:
    - "Profile validation returns WECHAT_NOT_RUNNING when no running WeChat process is supplied."
    - "Profile validation passes only when build, full dylib SHA-256, and arm64 slice SHA-256 all match the verified constants."
    - "Profile validation fails closed before attach/read/send when any hash or build field mismatches."
  artifacts:
    - path: "native-worker/phase0_worker.py"
      provides: "Exact profile gate implementation"
      contains: "WECHAT_DISK_PROFILE_MATCH"
    - path: "scripts/phase0_validate.sh"
      provides: "Profile gate validation wiring"
      contains: "profile-gate"
  key_links:
    - from: "scripts/phase0_validate.sh"
      to: "native-worker/phase0_worker.py"
      via: "worker profile-gate subcommand"
      pattern: "profile-gate"
---

<objective>
Implement the exact WeChat profile gate used before any key extraction or native send operation.

Purpose: Prevent profile drift from reaching local worker operations.
Output: Python worker profile-gate command with verified constants, full dylib hash, arm64 slice hash, no-process classification, and validation script wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@Agent.md
@native-worker/phase0_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define verified WeChat profile constants</name>
  <files>native-worker/phase0_worker.py</files>
  <read_first>Agent.md, native-worker/phase0_worker.py</read_first>
  <action>In `native-worker/phase0_worker.py`, define exact constants `EXPECTED_BUILD = "269111"`, `EXPECTED_FULL_DYLIB_SHA256 = "c2a4794b343625a8013752095e76bc688acd42d48f30001a255620db2fc542a9"`, `EXPECTED_ARM64_SLICE_SHA256 = "da53625065d283d748f959627f5e4d724eb786f2911f49c91a72cc12f17d30f7"`, `WECHAT_PATH = "/Applications/WeChat.app/Contents/MacOS/WeChat"`, and `WECHAT_DYLIB_PATH = "/Applications/WeChat.app/Contents/Resources/wechat.dylib"`.</action>
  <verify>rg -n "EXPECTED_BUILD|EXPECTED_FULL_DYLIB_SHA256|EXPECTED_ARM64_SLICE_SHA256|WECHAT_DYLIB_PATH" native-worker/phase0_worker.py</verify>
  <acceptance_criteria>
    - `native-worker/phase0_worker.py` contains `EXPECTED_BUILD = "269111"`.
    - `native-worker/phase0_worker.py` contains `c2a4794b343625a8013752095e76bc688acd42d48f30001a255620db2fc542a9`.
    - `native-worker/phase0_worker.py` contains `da53625065d283d748f959627f5e4d724eb786f2911f49c91a72cc12f17d30f7`.
    - `native-worker/phase0_worker.py` contains `/Applications/WeChat.app/Contents/Resources/wechat.dylib`.
  </acceptance_criteria>
  <done>Verified target profile is represented as worker constants.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed profile-gate command</name>
  <files>native-worker/phase0_worker.py</files>
  <read_first>native-worker/phase0_worker.py, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md</read_first>
  <action>Implement `profile-gate` subcommand. It must locate the running WeChat main process by exact executable path. If absent, emit JSON `status = "blocked"` and `code = "WECHAT_NOT_RUNNING"`. If `/Applications/WeChat.app/Contents/Resources/wechat.dylib` is absent, emit `status = "failed"` and `code = "WECHAT_DYLIB_MISSING"`. If build, full dylib SHA-256, or arm64 slice SHA-256 mismatches, emit `status = "failed"` and `code = "WECHAT_PROFILE_MISMATCH"` with evidence key `mismatches`. Only exact match emits `status = "passed"` and `code = "WECHAT_DISK_PROFILE_MATCH"`.</action>
  <verify>python3 native-worker/phase0_worker.py profile-gate</verify>
  <acceptance_criteria>
    - `native-worker/phase0_worker.py` contains `WECHAT_NOT_RUNNING`.
    - `native-worker/phase0_worker.py` contains `WECHAT_DYLIB_MISSING`.
    - `native-worker/phase0_worker.py` contains `WECHAT_PROFILE_MISMATCH`.
    - `native-worker/phase0_worker.py` contains `WECHAT_DISK_PROFILE_MATCH`.
    - `python3 native-worker/phase0_worker.py profile-gate` exits 0 when it reports `WECHAT_NOT_RUNNING` as a blocked terminal state.
  </acceptance_criteria>
  <done>Profile gate fails closed for no-process and mismatch states before native operations.</done>
</task>

<task type="auto">
  <name>Task 3: Wire profile gate into validation</name>
  <files>scripts/phase0_validate.sh</files>
  <read_first>scripts/phase0_validate.sh, native-worker/phase0_worker.py</read_first>
  <action>Update `scripts/phase0_validate.sh` so `run_worker` calls `scripts/phase0_worker.sh profile-gate` between `probe-process` and `attach-smoke`. The full validation must accept blocked `WECHAT_NOT_RUNNING` as an explicit terminal gate, not as a failed fallback.</action>
  <verify>scripts/phase0_validate.sh --worker</verify>
  <acceptance_criteria>
    - `scripts/phase0_validate.sh` contains `profile-gate`.
    - `scripts/phase0_validate.sh --worker` output contains either `WECHAT_DISK_PROFILE_MATCH` or `WECHAT_NOT_RUNNING`.
    - `scripts/phase0_validate.sh --worker` exits 0 when WeChat is not running and the worker reports blocked gates.
  </acceptance_criteria>
  <done>Unified validation exercises the exact profile gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 native-worker/phase0_worker.py profile-gate` exits 0 with either `WECHAT_DISK_PROFILE_MATCH` or blocked `WECHAT_NOT_RUNNING`.
- [ ] `scripts/phase0_validate.sh --worker` exits 0.
- [ ] `rg -n "WECHAT_NOT_RUNNING|WECHAT_PROFILE_MISMATCH|WECHAT_DISK_PROFILE_MATCH" native-worker/phase0_worker.py` finds all three codes.
- [ ] `rg -n "/bin/sh|uploadappattach|sendappmsg|Hermes|clipboard|key simulation|share sheet" app native-worker scripts -g '!scripts/phase0_validate.sh'` returns no matches.
</verification>

<success_criteria>
- Exact WeChat profile constants are present in the worker.
- No running WeChat, mismatch, and exact-match states are all represented.
- Worker validation runs profile gate before attach smoke.
- No fallback transport is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-02-SUMMARY.md`.
</output>
