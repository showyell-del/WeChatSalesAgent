---
phase: 00-commercial-delivery-and-exact-profile-gate
plan: 02
type: execute
wave: 1
depends_on: ["00-01"]
files_modified:
  - Sources/AgentCore/WeChatProfileGate.swift
  - Tests/AgentCoreTests/WeChatProfileGateTests.swift
autonomous: true
requirements:
  - Exact WeChat 4.1.11.55 build 269111 profile
user_setup: []
must_haves:
  truths:
    - "Profile validation returns WECHAT_NOT_RUNNING when no running WeChat process is supplied."
    - "Profile validation passes only when version, build, full dylib SHA-256, and arm64 slice SHA-256 all match the verified constants."
    - "Key/read/send preflight exposes a single allow/deny result and denies all mismatch states."
  artifacts:
    - path: "Sources/AgentCore/WeChatProfileGate.swift"
      provides: "Exact profile gate implementation"
      contains: "WeChatProfileGate"
    - path: "Tests/AgentCoreTests/WeChatProfileGateTests.swift"
      provides: "Profile gate tests"
      contains: "WECHAT_NOT_RUNNING"
  key_links:
    - from: "Sources/AgentCore/WeChatProfileGate.swift"
      to: "Sources/AgentCore/Diagnostics.swift"
      via: "DiagnosticEvent terminal result"
      pattern: "DiagnosticEvent"
---

<objective>
Implement the exact WeChat profile gate used before any key extraction or native send operation.

Purpose: Prevent profile drift from reaching native operations.
Output: Typed profile model, verified constants, fail-closed validation result, and tests for match/mismatch/no-process states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md
@.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-RESEARCH.md
@Agent.md
@Sources/AgentCore/Diagnostics.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define verified WeChat profile constants and model</name>
  <files>Sources/AgentCore/WeChatProfileGate.swift, Tests/AgentCoreTests/WeChatProfileGateTests.swift</files>
  <read_first>Agent.md, Sources/AgentCore/Diagnostics.swift</read_first>
  <action>Create `public struct WeChatProfile: Equatable` with properties `processPath`, `architecture`, `version`, `build`, `fullDylibSHA256`, and `arm64SliceSHA256`. Create `public enum WeChatProfileConstants` with exact constants: `version = "4.1.11.55"`, `build = "269111"`, `fullDylibSHA256 = "c2a4794b343625a8013752095e76bc688acd42d48f30001a255620db2fc542a9"`, and `arm64SliceSHA256 = "da53625065d283d748f959627f5e4d724eb786f2911f49c91a72cc12f17d30f7"`. Add tests asserting every constant equals the value in `Agent.md`.</action>
  <verify>swift test --filter WeChatProfileGateTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `4.1.11.55`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `269111`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `c2a4794b343625a8013752095e76bc688acd42d48f30001a255620db2fc542a9`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `da53625065d283d748f959627f5e4d724eb786f2911f49c91a72cc12f17d30f7`.
    - `swift test --filter WeChatProfileGateTests` exits 0.
  </acceptance_criteria>
  <done>Verified target profile is represented as code constants and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed profile validation</name>
  <files>Sources/AgentCore/WeChatProfileGate.swift, Tests/AgentCoreTests/WeChatProfileGateTests.swift</files>
  <read_first>Sources/AgentCore/WeChatProfileGate.swift, Sources/AgentCore/Diagnostics.swift, .planning/phases/00-commercial-delivery-and-exact-profile-gate/00-CONTEXT.md</read_first>
  <action>Implement `public struct WeChatProfileGate` with `public func validate(_ profile: WeChatProfile?) -> DiagnosticEvent`. If profile is nil, return blocked event with `code = "WECHAT_NOT_RUNNING"` and `step = "wechat_profile"`. If any field mismatches, return failed event with `code = "WECHAT_PROFILE_MISMATCH"` and evidence keys for every mismatched field. Only exact match returns passed event with `code = "WECHAT_PROFILE_MATCH"`. Add `public func allowsNativeOperation(_ profile: WeChatProfile?) -> Bool` that returns true only for exact match.</action>
  <verify>swift test --filter WeChatProfileGateTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `WECHAT_NOT_RUNNING`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `WECHAT_PROFILE_MISMATCH`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `WECHAT_PROFILE_MATCH`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `allowsNativeOperation`.
    - Test file contains `testNilProfileBlocksNativeOperation`.
    - Test file contains `testHashMismatchFailsNativeOperation`.
    - Test file contains `testExactProfileAllowsNativeOperation`.
  </acceptance_criteria>
  <done>Profile gate fails closed for no-process and mismatch states before native operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add process-probe parsing boundary</name>
  <files>Sources/AgentCore/WeChatProfileGate.swift, Tests/AgentCoreTests/WeChatProfileGateTests.swift</files>
  <read_first>Sources/AgentCore/ProcessRunner.swift, Sources/AgentCore/WeChatProfileGate.swift</read_first>
  <action>Add `public struct WeChatProcessProbe` with initializer `init(runner: ProcessRunning)` and method `public func currentProfile() -> Result<WeChatProfile, DiagnosticEvent>`. For Phase 0, implement command boundary and explicit no-process classification using `/bin/ps` with arguments `["-axo", "pid,command"]` through `ProcessRunning`, but do not parse hashes from shell pipelines. If output lacks `/Applications/WeChat.app/Contents/MacOS/WeChat`, return `.failure(.blocked(step: "wechat_profile", code: "WECHAT_NOT_RUNNING", ...))`. Leave exact live hash collection to the validation script in Plan 04, but keep validation API ready for a populated `WeChatProfile`.</action>
  <verify>swift test --filter WeChatProfileGateTests</verify>
  <acceptance_criteria>
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `struct WeChatProcessProbe`.
    - `Sources/AgentCore/WeChatProfileGate.swift` contains `/bin/ps`.
    - `Sources/AgentCore/WeChatProfileGate.swift` does not contain `/bin/sh`.
    - Test file contains `testProbeReportsWeChatNotRunning`.
    - `swift test --filter WeChatProfileGateTests` exits 0.
  </acceptance_criteria>
  <done>Runtime no-process detection exists and reports a distinct blocked state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter WeChatProfileGateTests` exits 0.
- [ ] `rg -n "WECHAT_NOT_RUNNING|WECHAT_PROFILE_MISMATCH|WECHAT_PROFILE_MATCH" Sources/AgentCore Tests/AgentCoreTests` finds all three codes.
- [ ] `rg -n "/bin/sh|uploadappattach|sendappmsg|Hermes|clipboard|key simulation|share sheet" Sources Tests` returns no matches.
</verification>

<success_criteria>
- Exact WeChat profile constants are present in code.
- No running WeChat, mismatch, and exact-match states are all tested.
- Native operations have one preflight allow/deny gate.
- No shell pipeline or fallback transport is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/00-commercial-delivery-and-exact-profile-gate/00-02-SUMMARY.md`.
</output>
