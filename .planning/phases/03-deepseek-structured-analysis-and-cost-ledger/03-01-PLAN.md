# Phase 3 Plan

1. Lock the direct DeepSeek HTTPS, strict schema, business-truth, privacy, cost, and evaluation contract.
2. Implement local candidate/evidence selection and conservative cost estimation.
3. Implement one-call JSON-mode transport, strict envelope/Pydantic/evidence/business validation, and zero retry/repair.
4. Persist staging runs, atomic lead result plus call ledger, exact provider usage JSON, price snapshots, and `Decimal` actual cost.
5. Verify the full pipeline against a one-call local provider fixture and expose live API Key/data-transfer gates.
