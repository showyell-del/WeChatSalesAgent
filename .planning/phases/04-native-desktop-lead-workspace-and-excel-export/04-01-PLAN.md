# Phase 4 Plan: Native Lead Workspace and Excel Export

## Goal

Publish a native AppKit merchant workspace and a real filterable Excel activation workbook from the same immutable published analysis run.

## Tasks

1. Build one strict workspace snapshot query over the published analysis, successful AI calls, facts, and immutable evidence.
2. Replace the placeholder AppKit window with dashboard KPIs, native filters, lead table, evidence detail, editable activation draft, and export action.
3. Build the workbook with the supported spreadsheet runtime, typed dates/numbers, filters, validation, formulas, conditional formats, and visual verification.
4. Fail closed when no published analysis exists; never display fixtures or stale corpus data as analyzed leads.
5. Verify native UI loading, rendered UI, workbook formulas, rendered workbook sheets, archive structure, and end-to-end export.

## Verification

- `scripts/phase4_validate.sh`
- `git diff --check`
